% ===========================================================================
%  Who wrote this and where it lives.
%
%  These three macros are the ONLY place the author's identity appears in the
%  layout (running heads, footers, title pages, slide footers).  If you fork
%  this repository, change them here and every document follows.
% ===========================================================================

\providecommand{\npauthor}{Hendrik Möller}
\providecommand{\npauthorurl}{https://github.com/Hendrik-code/numprog_script}
\providecommand{\npauthormail}{hendrik.moeller[AT]tum.de}

% Die URL, wie sie *gesetzt* wird (Fusszeile, Vorwort, Titelfolie).
% \npauthorurl selbst muss unangetastet bleiben -- es ist das Linkziel von
% \href.  Ausgeschrieben im Text scheitert es aber an Sonderzeichen wie dem
% Unterstrich in "numprog_script": der ist fuer TeX ein Tiefstellungszeichen
% und beendet die Uebersetzung mit "Missing $ inserted".  \detokenize macht
% daraus wieder ein gewoehnliches Zeichen.
\providecommand{\npauthorurltext}{\expandafter\detokenize\expandafter{\npauthorurl}}
